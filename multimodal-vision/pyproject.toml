[project]
name = "multimodal-vision"
version = "0.1.0"
description = "多模态图片理解与生成工具，基于 OpenAI 兼容协议"
readme = "SKILL.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "pillow>=10.0.0",
]

[project.scripts]
vision = "main:main"
